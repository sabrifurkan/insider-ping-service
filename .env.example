# Example environment variables for the ping service.
# Copy this file to `.env` and adjust values locally. Never commit the real `.env`.

# The HTTP port the service listens on. Defaults to 8080 if unset.
PORT=8080
